import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Brand
  static const primary = Color(0xFF6C63FF);
  static const primaryDark = Color(0xFF4B44CC);
  static const secondary = Color(0xFF03DAC6);
  static const accent = Color(0xFFFF6584);

  // Backgrounds
  static const background = Color(0xFF0F0F1A);
  static const surface = Color(0xFF1A1A2E);
  static const surfaceVariant = Color(0xFF252541);
  static const cardBackground = Color(0xFF1E1E35);

  // Text
  static const textPrimary = Color(0xFFF0F0FF);
  static const textSecondary = Color(0xFFAAAAAA);
  static const textDisabled = Color(0xFF555577);

  // Status
  static const success = Color(0xFF4CAF50);
  static const error = Color(0xFFCF6679);
  static const warning = Color(0xFFFFB74D);

  // Gradient
  static const gradientStart = Color(0xFF6C63FF);
  static const gradientEnd = Color(0xFF03DAC6);
}
